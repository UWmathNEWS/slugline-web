//Taken from DraftInlineStyleType in Draft JS
export enum InlineStyles {
  // Draft JS defaults
  Bold = "BOLD",
  Italic = "ITALIC",
  Underline = "UNDERLINE",
  Strikethrough = "STRIKETHROUGH",
  Code = "CODE"
}

// Taken from ComposedEntityType in Draft JS
export enum EntityTypes {
  // Draft JS defaults
  Link = "LINK",
  Token = "TOKEN",
  Photo = "PHOTO",
  Image = "IMAGE"
}
